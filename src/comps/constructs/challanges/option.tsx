import Preact from "preact"
import Button from "../../../components/buttons/Button"

type props = {
    selected : boolean,
    true : boolean,
}

const option : Preact.FunctionComponent<props> = ({

}) => {
    return(
        <main>
            option
        </main>
    )
}

export default option
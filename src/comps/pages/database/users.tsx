
import Preact from "preact"

type props = {
    
}

const users : Preact.FunctionComponent<props> = ({

}) => {
    return(
        <main>
            users
        </main>
    )
}

export default users

import Preact from "preact"

type props = {
    
}

const lesson : Preact.FunctionComponent<props> = ({

}) => {
    return(
        <main>
            lesson
        </main>
    )
}

export default lesson